exchanges:
  - name: inputModel
    resources: [Model,ModelSource,Log,Refresh]
    # from: fake
    # to: back
    # protocol: dp
    # parameters:
    #   fakeData: 'resolve:modelFake'
    # from: external
    # to: back
    # protocol: rest
    # parameters:
    #   url: "http://localhost:1112"
    #   resourceMap:
    #     Model: models
    from: external
    to: back
    annotations:
      implementation: 'resolve:dp/ModelDP'
    protocol: rest
    #MARK 是在这里设置还是在implementation设置？ 感觉最好是在这里设置一个环境变量。
    #MARK 这里跟implementation model有一定的联系。毕竟这个url需要被实现。
    #MARK 是否需要跨模型的变量机制？
    parameters:
      url: "http://localhost:1112"
  - name: modelDetails
    resources: [Entity,PartsByType]
    from: back
    to: back
    protocol: dp 
    annotations:
      implementation: 'resolve:dp/ModelDetailDP'